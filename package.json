{
  "name": "bookmark-brain",
  "version": "0.1.0",
  "description": "Local-first knowledge base from X bookmarks and screenshots, exposed via MCP",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "bookmark-brain": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "setup": "npm run build && node scripts/install-cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "chokidar": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
