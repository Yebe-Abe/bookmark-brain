{
  "name": "bookmark-brain",
  "version": "0.2.0",
  "description": "Pull X bookmarks, process with Claude, save as markdown",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "bookmark-brain": "dist/index.js"
  },
  "scripts": {
    "build": "rm -rf dist && tsc",
    "dev": "tsc --watch",
    "start": "node dist/index.js",
    "setup": "node scripts/install-cli.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.6.0"
  }
}
